#+TITLE: rsx-rs Documentation
#+AUTHOR: Rohit Goswami, Ruhila Goswami
#+OPTIONS: toc:nil num:nil title:nil author:nil

#+begin_export rst
.. raw:: html

   <div class="rc-hero">
     <img class="light-only rc-hero-logo" src="_static/logo-light.svg" width="460" height="120" alt="rsx" />
     <img class="dark-only rc-hero-logo" src="_static/logo-dark.svg" width="460" height="120" alt="rsx" />
     <p class="rc-hero-tagline">RAD-seq sex determination — streaming kernels, Bayesian marker evidence, CLI plus Python (<code>pyrsx</code>) and R (<code>rsxr</code>) bindings, RADSex-compatible TSV.</p>
   </div>

========
``rsx``
========

Rust toolkit for **RAD-seq marker analysis** and sex determination. Bounded-memory
streaming, Bayesian triage, and language bindings over a shared C API. Command-compatible
with `RADSex <https://github.com/RomainFeron/RADSex>`_ so prior biological results stay comparable.

.. important::

   **New here?** → :doc:`tutorials/quickstart`

   **Python / R / C surfaces?** → :doc:`reference/bindings`

   **In-process R package?** → :doc:`howto/r-integration` · ``rsx-r/README.md``

.. grid:: 1 1 2 2
   :gutter: 2

   .. grid-item-card:: Quickstart
      :link: tutorials/quickstart
      :link-type: doc

      Build the CLI, run ``process`` / ``signif`` / ``triage``, try ``pyrsx`` and ``rsxr``.

   .. grid-item-card:: Language bindings
      :link: reference/bindings
      :link-type: doc

      Parity matrix and multi-language triage examples (CLI, Python, R, C).

   .. grid-item-card:: Commands
      :link: reference/commands
      :link-type: doc

      Full CLI reference for RADSex-compatible and Bayesian commands.

   .. grid-item-card:: Architecture
      :link: explanation/architecture
      :link-type: doc

      Streaming kernels, evidence layer, and how bindings sit on the C API.
#+end_export

* Features

- Original RADSex-compatible commands: =process=, =distrib=, =signif=,
  =depth=, =freq=, =map=, =subset=
- Sex-linked marker evidence: strict calls, Bayes factors, posterior
  P(sex-linked), penetrance, bias direction, marker class (=triage=)
- =merge= (external sort for 75M+ tables) and streaming =pca=
- Byte-identical TSV to C++ RADSex when groups are specified explicitly
- Bounded-memory streaming on multi-GB tables; =rayon= parallelism
- Optional Parquet for merge (=--features parquet-io=)
- Bindings: Python (=pyrsx=), R (=rsxr=), C (=rsx.h= / cargo-c)
- Cross-platform Linux / macOS / Windows (map feature varies)

* Quick start

#+begin_src bash
git clone https://github.com/HaoZeke/rsx-rs.git
cd rsx-rs
cargo build --release
# or: pixi run build

pip install pyrsx
# R (needs cargo): pak::pak("HaoZeke/rsx-rs/rsx-r")

rsx process -i reads/ -o markers.tsv -T 8
rsx triage -t markers.tsv -p popmap.tsv -o triage.tsv -d 10 -G M,F
#+end_src

#+begin_export rst
.. toctree::
   :caption: Getting started
   :maxdepth: 1

   tutorials/quickstart

.. toctree::
   :caption: Reference
   :maxdepth: 2

   reference/bindings
   reference/rust-api
   reference/c-api
   reference/commands
   reference/glossary

.. toctree::
   :caption: How-to guides
   :maxdepth: 1

   howto/r-integration
   howto/faq
   howto/troubleshooting

.. toctree::
   :caption: Explanations & meta
   :maxdepth: 1

   explanation/architecture
   explanation/benchmarks
   explanation/citation
   changelog
   code_of_conduct
   used_by
#+end_export
