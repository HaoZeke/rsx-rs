#!/bin/sh
rm -f src/Makevars
# Keep staged src/rust/rsxcore (path dep for R CMD build); drop build artifacts only.
rm -Rf src/rust/.cargo src/rust/vendor src/rust/target src/.cargo src/.cargo-home .cargo-home
