[workspace]
members = ["rsx-core", "rsx-cli", "rsx-python"]
resolver = "2"

[profile.release]
lto = "thin"
codegen-units = 1

# cargo-dist builds (cross-platform, no -C target-cpu=native)
[profile.dist]
inherits = "release"
